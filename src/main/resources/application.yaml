spring:
  h2:
    console:
      enabled: true
  datasource:
    initialization-mode: always
    url: jdbc:h2:file:C:/Users/Admin/Downloads/FinalProjectApplication/FinalProjectApplication/src/main/resources/db_save/db_save
    username:
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
